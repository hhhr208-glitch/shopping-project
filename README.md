i still work on this after finishig i gonna update read me file ,  so yep for now in order to see progress so far  you can visit https://shopping-project-14.onrender.com

  one update : "
## Performance Note

Some requests may take 1–2 seconds when using the free-tier cloud database.  
This delay is caused by cold starts and limited resources on the free plan — it is **not due to inefficient code**.  
When using a local database (SQLite), the same requests complete in under 200 ms.
